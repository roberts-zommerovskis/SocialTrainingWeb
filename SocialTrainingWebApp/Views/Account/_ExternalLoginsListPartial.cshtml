@model SocialTrainingWebApp.Models.ExternalLoginListViewModel
@using Microsoft.Owin.Security


@{
    var loginProviders = Context.GetOwinContext().Authentication.GetExternalAuthenticationTypes().ToList();

    using (Html.BeginForm("ExternalLogin", "Account", new { ReturnUrl = Model.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal" }))
    {
        @Html.AntiForgeryToken()

        {
            AuthenticationDescription p = loginProviders[0];
            <div class="col-xs-9 col-xs-offset-1-5 col-sm-7 col-sm-offset-2-5 col-md-7 col-md-offset-2-5 col-lg-9 col-lg-offset-1-5">
                <button type="submit" id="@p.AuthenticationType" style="font-size:20px; background:#fff; color: #1d89ff; border: 1px solid #1d89ff;" name="provider" value="@p.AuthenticationType" title="Log in using your @p.Caption account" class="btn-block bttn-jelly bttn-sm bttn-primary">
                    <span aria-hidden="true" class="vismaicon vismaicon-google"></span>
                    @p.AuthenticationType
                </button>
            </div>
        }

    }
}
